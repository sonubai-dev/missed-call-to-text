# Backend Environment Variables
NODE_ENV=development
PORT=4000
HOST=0.0.0.0
JWT_SECRET=misscall_jwt_super_secret_signing_key_2026

# PostgreSQL (Optional - local in-memory fallback active by default)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/misscall_assistant

# Webhook Security
WEBHOOK_SECRET=whsec_missed_call_android_secret_key_v1
META_VERIFY_TOKEN=meta_whatsapp_verify_token_secure
META_APP_SECRET=meta_app_secret_hex_signing_key

# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

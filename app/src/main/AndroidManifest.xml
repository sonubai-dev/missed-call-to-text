<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Telephony, SMS & Call Detection Permissions -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Foreground Service & Background Automation Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Network permissions for local-first direct WhatsApp Cloud API / webhook dispatcher -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Package Visibility for WhatsApp and WhatsApp Business -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".MissCallAssistantApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MissCallWhatsAppAssistant"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MissCallWhatsAppAssistant"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Telecom Call Screening Service (Play Store Standard) -->
        <service
            android:name=".telephony.screening.MissedCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- Foreground Monitoring Service -->
        <service
            android:name=".telephony.service.CallMonitoringService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitoring missed calls and scheduling automated WhatsApp follow-up workflows for business calls." />
        </service>

        <!-- Phone State Receiver (Protected with BROADCAST_PHONE_STATE to prevent spoofing) -->
        <receiver
            android:name=".telephony.receiver.PhoneStateReceiver"
            android:permission="android.permission.BROADCAST_PHONE_STATE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver -->
        <receiver
            android:name=".telephony.receiver.BootReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Notification Action Receiver -->
        <receiver
            android:name=".notifications.receiver.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.misscall.whatsappassistant.ACTION_SEND_NOW" />
                <action android:name="com.misscall.whatsappassistant.ACTION_IGNORE" />
                <action android:name="com.misscall.whatsappassistant.ACTION_DISMISS" />
            </intent-filter>
        </receiver>

        <!-- Native SIM SMS Status Receiver -->
        <receiver
            android:name=".telephony.sms.SmsStatusReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.misscall.whatsappassistant.ACTION_SMS_SENT" />
                <action android:name="com.misscall.whatsappassistant.ACTION_SMS_DELIVERED" />
            </intent-filter>
        </receiver>

        <!-- Disable default WorkManager Initializer for on-demand Hilt WorkerFactory -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
